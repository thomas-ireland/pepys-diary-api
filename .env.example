# Copy to .env for local development. Matches the credentials in
# docker-compose.yml, so `docker compose up -d` then `npm run db:migrate`
# works with no further setup.
DATABASE_URL="postgresql://pepys:pepys@localhost:5432/pepys?schema=public"
